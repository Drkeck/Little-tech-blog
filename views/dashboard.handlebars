<article>
    <a class="btn" href="/newpost">New Post</a>
</article>

<article>
    <ul>
        {{#each posts as |post|}}
        <li>
            {{> edit-posts post}}
        </li>
        {{/each}}
    </ul>
</article>